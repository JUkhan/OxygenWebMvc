@echo off          //this makes it so you have an empty cmd window on startup
node index          // this runs the .class file
pause              //this prevents the window from instantly closing after program end